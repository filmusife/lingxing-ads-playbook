---
name: lingxing-ads-playbook
description: 用领星 MCP 按 ASIN+站点拉取最近30天真实广告与 Listing 数据，按产品功能差、上架阶段、搜索频率排名和配置快照冷却锁做深度 SP/SBV 分析，并输出可执行的完整或 delta 验收手册。适用于任意品类（家电、配件、消耗品等），不绑定研磨器样例。在用户给 ASIN 和国家/站点、提到领星广告、广告组合、SP 广告优化、搜索词否定、基础竞价、位置溢价、历史调整、或输入 /lingxing-ads、/广告调整 时使用。不要用于写 Listing 文案、品类选品、或没有活数据的空泛广告理论。
metadata:
  short-description: Deep Lingxing ads audit and full adjustment playbook
---

# 领星广告调整手册

给 ASIN + 国家后，必须用领星活数据做**产品匹配优先**的广告诊断，输出一份别人能直接照着改的手册。禁止只看组合/活动 ACOS 就否定或加码。

**目的不是把 ACOS 做低，而是提升 Listing 搜索权重和自然流量。** ACOS 只是约束。不要把机制写成“A9”或“亚马逊最新官方公式”；用可观察的 query 转化、相关流量、小类 BSR 来解释。

## 何时用

- 用户给了 ASIN，并给了国家/站点（美国站、US、UK、DE…）或店铺名
- 要看广告组合、广告活动、投放词、搜索词、自动/商品投放、位置溢价、基础竞价
- 要的是调整建议，不是“ACOS 高就否”
- 同一 ASIN 7 天内重跑、用户说“我刚按上次改过了”，仍然用本 skill，但改出 **delta/验收**，不要当第一次诊断

不要用来写标题五点（转 `amazon-listing-agent` / `amazon-listing-penalty-audit`），也不要在领星连不上时编造报表数字。

## 铁律

1. **先产品，后 ACOS。** 从 Listing 属性判断本品 IS / IS NOT，再决定加码、保留、降价、否定。错品词即使出过单也优先否定。**强相关词数据差 ≠ 否定**：功能卡判定 IS、页面能支撑时，差数据先当投放/位置/Listing 没命中，不要当垃圾词清掉。
2. **先功能差，后价格。** 竞品广告能不能打，先看形态/架构和页面可见的功能差，再看评分/评分数，价格只是加分项。禁止写“价格差不多所以保留 ASIN”。价格接近但打不过时，先填赢面矩阵，再在「降基础竞价观察」和「暂停/否定」里选，不要跳到否词。
3. **先定阶段，再定架构和容忍度。** 用上架时间、首单、评论、销量斜率判断冷启动/爬坡/成熟/防御/高库存/缺货。新品按老品去否词、打头部竞品，算失败。
4. **看全层数据。** 至少覆盖：店铺/Profile、Listing、FBA 库存、活动（含 TOS/PP/ROS、预算、竞价策略）、广告组默认基础竞价、关键词（匹配方式 + 基础竞价 + CPC）、搜索词、自动/ASIN 投放、广告商品、**关键词分析 `searchrank`（搜索频率排名）**。缺哪层就标 `DATA_MISSING`，不要用活动汇总代替，也不要编造周搜索量。
5. **先改位置溢价，再改基础竞价。** 低基础竞价 + 超高 TOS 会让 CPC 远高于基础竞价。同一天不要既砍词又大幅加薪。
6. **位置溢价只允许一套数。** 先写总表，后面每个活动的 TOS/PP/ROS 必须与总表完全相同。写完后逐行对账，禁止总表降、详情里又留高溢价。
7. **对外一律写「基础竞价」。** 不要对用户说 BID/bid。广告组层写「默认基础竞价」，关键词层写「基础竞价」。API 字段仍是 `bid`，只留在脚本内部。
8. **默认分析窗口是最近 30 天**（昨天往前）。否定词必须用 30 天样本，不要用 7/14 天就否。用户明确要求短窗口才改。**30 天只用于诊断存量问题，不用于验收刚改过的杠杆。** 刚改过的 TOS/基础竞价/预算，必须另看改后 3–7 天窗口，否则 30 天会被旧设置污染。
9. **默认只出建议，不写回广告。** 领星有 `put_campaigns_sp`、`advertising_add_negative_keywords` 等写接口；只有用户明确说“帮我改后台/提交否定词”才调用写工具。
10. **永远不要打印或回显** `X-Mcp-Key`、Open API secret、config 里的 token。
11. 用户用中文就出中文手册。
12. **冷却只认自变量，不认 CPC。** 运营真正改的是 TOS/PP/ROS、日预算、启停、默认基础竞价、关键词/投放基础竞价。CPC 和花费是结果变量，流量结构、竞品出价、位置组合都会让它们自己跳，**不能当“运营改过了”的主证据**。证据优先级：配置快照差分（跨次运行比对当前控制变量）> 领星运营日志（给改动日）> 用户口述 > 本地上次手册。逐日报表里的 TOS/bid/预算永远是此刻快照。否定投放不锁溢价。7 天内重跑出 **delta/验收**。
13. **否词前先对功能卡。** 用户搜索词/投放词即使 30 天 ACOS 差、0 单，只要形态/功能是本品 IS 且页面可见，默认动作是暂停、降基础竞价、修 TOS/Listing，而不是否定。只有功能卡写明「明确没有」或硬错品，才否。
14. **竞品 ASIN：赢面矩阵 → 再选降价或暂停。** 同形态 + 我方有可见差异化但投放 CVR 差 → 先降基础竞价/降 PP 观察。不同形态、对方主卖点我们没有、评分数碾压且无可见卖点 → 暂停/否定。禁止「价格差不多但打不过所以否」。
15. **大词差数据先查竞争和位置，不先否。** 用 `advertising_ad_analyze_keyword.searchrank`（ABA 搜索频率排名，数字越小量越大）判断是不是大词。领星没有周搜索量绝对值；缺 `searchrank` 就标 `DATA_MISSING`，用曝光作代理。头词/躯干词曝光很少或 TOS 份额极低 → 抬基础竞价或修位置结构；曝光已经很高但 CTR 低 → Listing/主图；CTR 高 CVR 低 → 功能可见性或页面，不否。

## 必做流程

连接与取数细节读 [references/mcp-connection.md](references/mcp-connection.md) 和 [references/tool-params.md](references/tool-params.md)。判定标准读 [references/analysis-framework.md](references/analysis-framework.md) 和 [references/feature-lifecycle.md](references/feature-lifecycle.md)。冷却与快照（任意品类）读 [references/cooldown-snapshot.md](references/cooldown-snapshot.md)。成稿结构用 [references/report-template.md](references/report-template.md)。

### 1. 接通领星

- 配置在 `~/.codex/config.toml` 的 `[mcp_servers.lingxing-mcp]`。
- 本会话若**没有**原生 MCP tools，不要停：用 `scripts/lx_client.py` 走 HTTP JSON-RPC（`help` / `search` / `action`）。
- 连不上就告诉用户怎么贴 MCP 配置（白名单以后再说），不要改用过期截图或仓库旧数冒充当前表现。

### 2. 取数

在当前工作目录跑（把 ASIN/国家换成用户给的）。脚本在 skill 根目录 `~/.codex/skills/lingxing-ads-playbook/scripts/`：

```bash
python ~/.codex/skills/lingxing-ads-playbook/scripts/fetch_ads_bundle.py --asin {ASIN} --country {US|UK|DE...} --outdir work/lx-ads
python ~/.codex/skills/lingxing-ads-playbook/scripts/analyze_ads_bundle.py --indir work/lx-ads --outdir work/lx-ads/analysis
```

Windows 把 `~/.codex` 换成 `%USERPROFILE%\.codex`。用户指定了店铺名就加 `--shop YourShop`。默认窗口是昨天往前 **30 天**。

`analyze_ads_bundle.py` 只产出证据表。**最终手册必须按模板手写完整建议**，不要把 tsv 当交付物。脚本分桶只用标题/品牌/ASIN 重叠，**没有品类硬编码**；手册按该 ASIN 功能卡重分。`keyword_analysis.tsv` 的 `searchrank` / `vol_band` / `hint` 是弱先验：`*_do_not_negate` 时禁止直接抄进否定列表。冷却以 `config_diff.tsv` + `cooldowns.tsv` 为准，禁止把“CPC 是基础竞价 3 倍 → 再降 TOS”抄进第 3 节。快照文件按 profile+ASIN 隔离，吸尘器不会吃到研磨器的锁。

取数失败时先 `search` 该 toolId 的 schema，按 [tool-params.md](references/tool-params.md) 的坑重试，不要直接放弃。

`fetch_ads_bundle.py` 会额外拉 `advertising_ad_analyze_keyword` 写成 `keyword_analysis.json`。分析脚本 join 到 `keywords.tsv` / `search_terms.tsv` / `diagnosis_hints.tsv`。这是搜索量代理，不是周搜索次数。

### 3. 建产品画像

从 Listing + 小类 BSR + 属性 + 标题 + `open_date` / `first_order_time`，写清：

- **阶段卡**：冷启动 / 爬坡 / 成熟 / 老品防御 / 高库存 / 缺货。依据上架、首单、评论、7/14/30 斜率、广告占比、库存，而不是“有单就是老品”
- **功能卡**：形态/架构、已验证功能、明确没有、差异化；每个差异化是否出现在主图/标题/前两点
- 本品是什么、卖点、价格栈（成交/划线/券）、评分/评分数、小类节点
- 本品**不是**什么（竞品形态、没有的功能、错误品类）
- 库存能否加码（可售天数、库龄、在途、日均销量走势）
- 广告单量 vs 总销量：广告占比过高时，自然流量靠 **相关 query 的转化**，不是靠乱扩词

用户若提供属性表，以属性表和后台 Listing 为准，不要用广告词反推功能。促销期 CVR 不能把阶段往成熟判。功能卡必须从**这一支 ASIN** 抽，咖啡机、吸尘器、研磨器、配件各写各的，禁止复用上一份手册的 IS NOT。

### 4. 先对上次手册（诊断前必做）

在写任何「TOS 再降一档 / 基础竞价再加一档」之前，必须完成对照。不要把本地手册当主证据。

按这个顺序找证据：

1. **配置快照差分（自变量）** `analysis/config_diff.tsv` 和 skill 目录 `state/{profile}_{ASIN}.json`。每次跑完自动留下当前 TOS/PP/ROS、日预算、启停、默认基础竞价、关键词/投放基础竞价。下次跑直接比对，看到 `TOS 160→90` 或 `基础竞价 0.50→0.45` 才算确定改过。这是机器写的几 KB 控制变量，不是运营日记，也不依赖某个 Codex 线程。
2. **领星运营日志** `ops_ads_events.tsv`。501 广告活动、502 广告组、504 投放给出**改动日和对象**；没有 before/after 数值。505 否定投放不锁溢价。
3. 用户口述，尤其是亚马逊后台改了但领星日志还没刷出来。
4. 本地上次手册只对照「改成」的具体数字；找不到也可以。
5. 当前后台活数据填「现在」列。

`daily_breaks.tsv` 的花费/CPC 跳变只用来拆改后观察窗口，**不要据此锁杠杆**。禁止把逐日行里的 TOS/基础竞价/日预算当成历史设置。

对每个杠杆（活动×位置溢价、活动×默认基础竞价、关键词基础竞价、日预算、启停）做三列对照：**上次「改成」 / 现在后台 / 判定**。判定只允许三种：

| 判定 | 怎么认 | 本次手册怎么写 |
|---|---|---|
| 已执行且冷却中 | 现在值 = 上次「改成」，或方向一致且幅度不小于上次；距改动不足 7 天（最少锁 3 天） | 「改成」写 **维持**。原因写冷却截止日，以及 30 天 CPC 仍含旧设置、不能再砍 |
| 上次建议未做 | 现在值仍接近上次手册的「现在」，几乎没动 | 可以再出一次旧建议，但必须标明“未执行的旧建议，不是新诊断” |
| 新证据才可动 | 冷却已满，且能拆出改后至少 3 个完整日（最好 7 日）的窗口 | 只用**改后窗口**的 CPC/CVR/订单/TOS 份额做二次调整；禁止用 30 天混合窗口 |

**冷却锁（非紧急）：**

- 位置溢价 TOS/PP/ROS、基础竞价、日预算：改动后锁 **7 天**，最短 3 天
- 同一活动 TOS 7 天内禁止连砍两次
- 冷却期内也不许把上次改动原路加回去，除非改后窗口已经证明核心大词日单腰斩

**紧急例外（随时可动，不受冷却）：**

- 硬 IS NOT 否定（以**本 ASIN 功能卡「明确没有」**为准，不要套别的品类词表）
- 暂停明显打不赢或错形态的 ASIN
- 同一 exact 在多个仓自相残杀、正在抢核心量
- 缺货 / IS NOT 活动还开着

**30 天污染（必须在报告里写出来）：**

TOS 2 天前从 250% 降到 160% 时，30 天 CPC 里大约还有 28 天旧 TOS。这时看到“CPC 仍是基础竞价 2–3 倍”再砍到 90%，是把旧病当成新病。正确表述：30 天 CPC 高，是窗口被旧溢价污染；验收看改后 3–7 天 CPC 是否回到新有效顶部竞价附近。

**重跑模式：**

- 上次手册距今 **< 7 天**：默认 **delta/验收**。不要当第一次诊断重写完整 TOS 矩阵。聊天先讲：哪些已经改了不要动、哪些上次没做、今天只做清洁类。
- 上次手册距今 **≥ 7 天**：可以出完整手册，但仍必须带「相对上次手册」表，禁止把已落地的杠杆包装成今天的新发现。

### 5. 诊断

对每个启用活动同时看：类型（SP/SBV/SD、手动/自动）、匹配、默认基础竞价、关键词基础竞价、TOS/PP/ROS、CPC、CVR、CTR、直接/间接订单、TOS 份额、搜索词泄漏、竞价策略（固定 vs 动态提高）。

把搜索词和投放词分到：核心相关、品牌、功能卖点、相邻能转（不扩）、错品、竞品品牌词、ASIN 对比。功能卖点必须已验证且页面可见。脚本 `buckets.tsv` / `waste.txt` 不够格单独否词。

ASIN / 竞品品牌：逐条走赢面矩阵（形态 → 对方主卖点我们有没有 → 可见差异化 → 评分数差 → 价格 → 实测 CVR）。价格接近不是保留理由，也不是否定理由。矩阵结论是进攻才加码；观察则降基础竞价；暂停才停。

漏斗：先匹配和阶段，再看 **searchrank/曝光代理**，再看曝光/CTR/CVR/TOS 份额/CPC，最后才是 ACOS。
- 强相关 + 大词 + 曝光少/TOS 份额极低 → 抬基础竞价或修位置，禁止否定
- 强相关 + 高 CTR + 低 CVR → 功能是否在页面可见；可见则降价观察，不可见则 Listing 配合，不否
- 强相关 + 小词 + 点击达暂停线仍 0 单 → 先暂停 7 天
- 只有功能卡 IS NOT 才否定，即使它是大词、即使偶然出过单
高 CTR 低 CVR 先查页面和功能，不要加价硬打。

冷却中的杠杆：30 天 CPC 再高，也只能写进「污染说明」，不能写进第 3 节「改成」。

### 6. 出完整手册或 delta 手册

聊天里给：目标、今天止血清单、明天基础竞价、核心否定词、3/7 天验收。若是 7 天内重跑，聊天还要先给「相对上次：锁哪些 / 补做哪些 / 今天不要动位置」。

完整或 delta 手册写到 `outputs/{ASIN}-{country}-ads-playbook.md`（或用户指定目录）。必须按 [report-template.md](references/report-template.md) 写全，禁止只交一张活动 ACOS 表。

写位置溢价时：先定总表，再把同一行数字复制进该活动详情。冷却中的行总表和详情都写 **维持**。不要在详情里“顺便改一版”。

## 交付标准

合格手册必须让运营不用再问就能改，至少包括：

- 产品画像：阶段卡 + 功能卡 + 权重目标（小类节点 + 自然流量，而不是“把 ACOS 做低”）
- **相对上次改动**：必须写证据来源（配置快照差分 / 运营日志 / 口述 / 本地手册）。`config_diff.tsv` 有 TOS/基础竞价变动就锁该杠杆；不要用 CPC 跳变当改过了
- 组合 **30 天**盘点 + 搜索词意图拆分（按功能卡，不按脚本样例正则）；刚改过的杠杆另附改后 3–7 天说明
- 投放词/搜索词带 **searchrank（或曝光代理）**；大词差数据必须写「竞争/位置/Listing」理由，不能只写 ACOS
- 竞品/ASIN 赢面矩阵（形态/功能/评分数/价格/CVR），不是价格单列；打不赢时写清是降基础竞价还是暂停
- 位置溢价总表（现在 → 改成 → 原因），冷却行「改成」= 维持，且与后文每个活动数字一致
- 每个活动：保留/暂停/降预算、默认基础竞价、关键词基础竞价、否定、迁词
- 可粘贴否定精准/词组（30 天样本，慎重；新品更慎重）
- 操作顺序（第 0 天止血，第 1 天调基础竞价，第 7 天再开暂停的收割仓）。冷却期内第 0 天不要重砍刚改过的 TOS
- 为什么这样改（相关搜索转化和自然流量，不是 ACOS 故事，也不要写成官方算法名）

缺搜索词、关键词层、上架时间、竞品评分数或 `searchrank` 时，明确写 `DATA_MISSING`，并降低建议强度。没有周搜索量就不要编一个。
