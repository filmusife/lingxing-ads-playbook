# 报告模板

写到 `outputs/{ASIN}-{country}-ads-playbook.md`。任意品类同一模板。聊天里给摘要 + 今天清单，不要只丢文件路径。

用户中文则整份中文。每个数字带窗口（默认 **30 天**）。建议必须写**现在值 → 改成 → 为什么**。对外写「基础竞价」，不要写 BID。冷却中的行「改成」必须写 **维持**，不要为了填表再造一套新溢价。

位置溢价只写一套：第 3 节总表是唯一真相。第 4 节每个活动的 TOS/PP/ROS 必须从总表复制，写完后对照一遍。

上次手册距今 < 7 天：改出 **delta/验收**，不要假装这是第一次诊断。聊天先讲：锁哪些、上次哪些没做、今天只做清洁类。

```markdown
# {ASIN} {国家} 广告调整手册
窗口：{start} 至 {end}（30天） | 店铺 {alias} | sid {sid} | profile {profile_id}
上次手册：{path 或 未找到，可空} | 云端最近广告改动日：{ops_log 日期或无} | 本报告模式：完整 / delta验收
改动证据：配置快照差分 / 运营日志 / 口述 / 本地手册。CPC 跳变不算改过设置

## 0. 产品与目标
- 阶段卡：冷启动 / 爬坡 / 成熟 / 老品防御 / 高库存 / 缺货。写上架日、首单、评论、7/14/30 斜率，以及为什么是这个阶段
- 功能卡：形态/架构、已验证功能、明确没有、差异化；差异化是否出现在主图/标题/前两点
- 本品是什么 / 不是什么（小类节点、价格栈：成交/划线/券、评分/评分数）
- 库存与销量走势（可售、可售天数、在途、广告单占比）
- 本次目标：提升相关搜索权重和自然流量；ACOS 是约束。写清要加码的 query 家族、不要去抢的节点。阶段不同目标不同：新品先相关出单，老品先清洁换自然流量
- 竞品广告总原则：按赢面矩阵，不按价格接近

## 0.5 相对上次手册（诊断前先填，禁止跳过）
杠杆 | 上次「改成」 | 现在后台 | 判定（已执行且冷却中 / 上次建议未做 / 新证据才可动） | 本次动作 | 冷却截止
---|---|---|---|---|---
{活动} TOS/PP/ROS | 快照/手册旧值 | 现在后台 | | 维持 或 补做旧建议 或 改后窗口二次调整 |
{活动} 默认基础竞价 | | | | |
{活动} 日预算 | | | | |
{活动} 启停 | | | | |

判定优先用 `config_diff.tsv` + `cooldowns.tsv`。快照差分写出 TOS/基础竞价变化才锁该杠杆。运营日志补改动日。花费/CPC 断点不锁杠杆。
找不到本地手册不要紧，有云端改动日照样锁。冷却中禁止用 30 天 CPC/ACOS 再砍同一杠杆。
7 天内重跑：这张表决定第 3 节和第 10 节，而不是 30 天 CPC。

## 1. 30 天盘点
- 组合花费 / 销售额 / ACOS / 订单 / CVR / CPC / CTR / TACOS
- 按意图桶拆搜索词（核心/品牌/功能/相邻/错品/ASIN）——按功能卡重分，不照抄脚本分桶
- 大词/竞争度：`searchrank`（ABA 搜索频率排名，越小量越大）或曝光代理。缺则 `DATA_MISSING`，禁止编周搜索量
- 结构问题：低基础竞价 + 高 TOS、重复投放、自动打错品类、预算花不满、动态加价、促销美化 CVR、大词曝光明显低于本品其他头词 等
- 本品搜索广告中位 CVR（后面 0 单阈值用这个，不用别的品类的绝对点击数）
- 若位置/基础竞价刚改过：单列改后 3–7 天 CPC/订单/TOS 份额。没有改后窗口就写「样本不够，维持」

## 1.5 竞品 / ASIN 赢面矩阵
活动或投放 | 形态是否相同 | 对方主卖点我们有吗 | 我们可见差异化 | 评分数差 | 价格 | 投放 CVR vs 搜索 CVR | 结论（进攻/观察=降基础竞价/暂停）
禁止只填价格。价格接近且暂时打不过 → 先观察降价，不要直接暂停。拿不到对方评论/价格就标 DATA_MISSING，用形态+功能+我方 CVR 兜底。

## 2. 操作顺序
第 0 天止血 → 第 1 天核心基础竞价与迁词 → 第 3/7 天验收。写明今天不要做什么。
日历相对**上一次改动**，不是每次重跑都从第 0 天重砍 TOS。冷却中的位置溢价/基础竞价/预算，今天不要做什么要写进这一节。
新品不要在第 0 天否相关词或新打头部 ASIN。缺货不要扩量。

## 3. 位置溢价总表（唯一标准）
活动 | 现在 TOS/PP/ROS | 改成 | 原因

本表定稿后再写第 4 节。后面禁止另给一套溢价。
ASIN 仓改成的 TOS/ROS 必须是 0，除非明确不是商品投放。
冷却中的行「改成」= **维持**，原因写冷却截止日和「30 天 CPC 含旧溢价」。不要把 160 再改成 90 来证明这份报告有工作量。

## 4. 活动级操作
按仓写：核心精准、品牌、功能、收割、发现、自动、SBV、ASIN。
每个启用活动都要有结论（保留/暂停/降预算）以及：
- 默认基础竞价、关键词基础竞价、匹配方式
- TOS/PP/ROS：与第 3 节该行完全相同
- 要迁走/否定/降价的词（带 30 天花费、订单、ACOS、CVR、**searchrank 或曝光代理**、**产品理由：形态/功能/阶段**）
- 强相关但数据差的词必须写：为什么不是否定（没打到点 / 大词竞争 / Listing 不可见）
暂停的活动也要写为什么，避免运营误开。
ASIN 活动的去留必须引用第 1.5 节矩阵结论；观察=降基础竞价，不是否定投放。

## 5. 目标架构
改完后保留哪几类仓，避免再开一堆单关键词超高 TOS 活动。
架构复杂度必须匹配阶段：冷启动极简，成熟期才角色齐全。

## 6. 可粘贴否定词
否定精准 + 否定词组。写清加到哪些活动。标明不要否定的词。
只放 30 天证据够硬的 **IS NOT**；相关但 0 单的先暂停，不要整批发否定。
`diagnosis_hints.tsv` 里 `*_do_not_negate` / `head_term_*` 的词禁止出现在本列表。
新品冷启动：否定列表更短，只放硬 IS NOT。

## 7. 为什么这样改
用相关 query 转化、自然流量、功能匹配、对比页胜率、阶段、库存约束解释。
不要只重复 ACOS，也不要写成“按 A9 / 官方最新算法”，也不要写成“价格接近所以能打”。

## 8. Listing 配合（可选，一两段）
广告词已经在打、但页面没有对应卖点或声称了没有的功能时才写。
功能可见性不足导致对比页打不赢时，写“先补主图/前两点，再开这个 ASIN”，不要靠加价硬打。

## 9. 3 天 / 7 天验收表
核心大词订单与 TOS 份额、错品花费、日均订单、小类 BSR、自然单占比。组合 ACOS 只作约束项。
促销若结束，单独看 CVR 是否回落，再决定核心基础竞价。
刚改过的杠杆：3/7 天看的是**改后窗口**，不是再出一张新的 TOS 总表。订单掉 >30% 才允许在冷却到期后动基础竞价，默认仍不加 TOS%。

## 10. 今天就做的清单
编号、可勾选、按顺序。
冷却中不要出现「按第 3 节改位置溢价」这种总开关，除非第 0.5 节里确有「上次建议未做」的行。
7 天内重跑的合格清单通常是：补否 IS NOT、暂停打不赢 ASIN、exact 防抢量、确认上次「改成」已落地。
```

## 写完自检

- 第 3 节与第 4 节同一活动的 TOS/PP/ROS 是否逐字相同
- 全文是否把基础竞价错写成了 BID
- 否定词是否都有 30 天样本和产品理由（形态/功能，不只是 ACOS），以及 searchrank/曝光
- 是否把强相关差数据词误放进了否定列表
- 是否写了阶段卡和功能卡
- ASIN 去留是否引用了赢面矩阵，而不是价格一句话；打不赢时是降基础竞价还是暂停
- 0 单阈值是否用了本品搜索 CVR，而不是固定 20 点击
- 是否填了第 0.5 节；7 天内有旧手册或用户口述时，冷却行是否写成维持
- 是否用 30 天 CPC 去砍 2–7 天内刚改过的 TOS/基础竞价（有则必须改回来）
- 第 10 节是否又把冷却中的溢价当成今天必做

## 不合格

- 只有活动 ACOS 排名和“暂停高 ACOS”
- 没有匹配方式、基础竞价、TOS
- 总表和详情溢价打架
- 否定词没有产品理由，或只用 7/14 天 0 单就否
- 建议无法执行（“优化一下关键词”）
- 把相邻能转的词当成核心大词加预算
- 把目标写成“降低 ACOS”或“按 A9 最新公式”
- 编造领星没返回的数字
- 只因为价格接近就保留竞品 ASIN
- 价格差不多、没看功能差就暂停/否定竞品 ASIN
- 强相关词因 ACOS 差、0 单或 CPC 高就否定
- 大词差数据却不看 searchrank / TOS 份额 / 曝光，直接当垃圾词
- 没写产品阶段就把新品按老品去否词、打头部竞品
- 编造领星没有返回的周搜索量
- 用促销期 CVR 去扩发现或把阶段判成熟
- 把页面上看不见的功能当成竞品页赢点
- 无视 3–7 天冷却，用 30 天 CPC/ACOS 对刚改过的 TOS/基础竞价再砍一刀
- 7 天内重跑却不出「相对上次手册」，把完整 TOS 矩阵当新任务
- 把上次已落地的「改成」又写成今天的新建议
- 用户已说「两天前改过了」，报告里完全不提历史调整
